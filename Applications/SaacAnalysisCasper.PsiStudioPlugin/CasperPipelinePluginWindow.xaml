<Window x:Class="SaacAnalysisCasper.PsiStudioPlugin.CasperPipelinePluginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SaacAnalysisCasper PsiStudio Plugin"
        Width="640"
        Height="420"
        WindowStartupLocation="CenterOwner">
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0"
               FontWeight="SemiBold"
               Text="Thin host-swap POC — Core Poc via inject (no Live remoting)."
               TextWrapping="Wrap" />

    <Grid Grid.Row="1" Margin="0,12,0,0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>
      <TextBlock Grid.Column="0"
                 VerticalAlignment="Center"
                 Margin="0,0,8,0"
                 Text="Run-config:" />
      <TextBox x:Name="RunConfigPathTextBox"
               Grid.Column="1"
               VerticalAlignment="Center"
               MinHeight="24" />
      <Button x:Name="BrowseRunConfigButton"
              Grid.Column="2"
              Margin="8,0,0,0"
              Padding="10,4"
              Content="Browse…"
              Click="BrowseRunConfig_Click" />
    </Grid>

    <DockPanel Grid.Row="2" Margin="0,12,0,0" LastChildFill="True">
      <Button x:Name="RunPocButton"
              DockPanel.Dock="Right"
              Margin="8,0,0,0"
              Padding="12,4"
              Content="Run POC"
              Click="RunPoc_Click" />
      <TextBlock x:Name="StatusTextBlock"
                 VerticalAlignment="Center"
                 Text="Ready" />
    </DockPanel>

    <TextBox x:Name="LogTextBox"
             Grid.Row="3"
             Margin="0,12,0,0"
             IsReadOnly="True"
             VerticalScrollBarVisibility="Auto"
             HorizontalScrollBarVisibility="Auto"
             FontFamily="Consolas"
             AcceptsReturn="True"
             TextWrapping="Wrap" />
  </Grid>
</Window>
